# PCB缺陷检测实验日志

> 项目：基于YOLOv8与边缘计算的PCB缺陷检测系统
> 实验人：陶郡奕
> 日期：2026/07/17

---

## 实验环境

| 项目 | 配置 |
|:----|:----|
| CPU | Intel Xeon / AMD |
| GPU | NVIDIA (CUDA可用) |
| 内存 | 16GB |
| 系统 | Windows 11 + WSL2 Ubuntu 20.04 |
| Python | 3.10 + ultralytics 8.4.83 |
| CUDA | 可用，显存约2GB |
| 地平线工具链 | Docker: openexplorer/ai_toolchain_ubuntu_20_x5_cpu:v1.2.8 |

---

## 数据集

**DeepPCB** — 开源PCB缺陷检测数据集

| 分集 | 图片数 | 标注数 |
|:----|:-----:|:-----:|
| 训练集 | 1275 | 8515 |
| 验证集 | 225 | 1498 |

**缺陷类别（校正后0-indexed）：**

| 索引 | 名称 | 说明 |
|:---:|:----|:----|
| 0 | open | 开路/断线 |
| 1 | short | 短路 |
| 2 | mousebite | 鼠咬缺角 |
| 3 | spur | 毛刺/突铜 |
| 4 | copper | 铜箔异常 |
| 5 | pin-hole | 针孔 |

### 标签问题修复

**发现：** DeepPCB原始标签是1-indexed（1=open, 2=short, ... 6=pin-hole），但YOLOv8要求0-indexed。训练数据没有做减1转换，导致：

| 原始标签 | 模型学到(0-index) | 应该是 |
|:-------:|:----------------:|:------:|
| 1 (open) | → short ❌ | open |
| 2 (short) | → mousebite ❌ | short |
| ... | → 全部错位 | ... |
| 6 (pin-hole) | → 越界丢弃 | pin-hole |

**修复：** 将所有标签文件类别号减1，共计校正8515个标注。

---

## 实验记录

### 实验1：标签错位训练（基线）

| 项目 | 值 |
|:----|:----|
| 模型 | YOLOv8n（错位标签） |
| Epochs | 200 |
| 最佳mAP50 | **0.279** |
| 测试图检测数 | 219个（几乎全误报） |

**问题：** open识别为short，short识别为mousebite，pin-hole未学到。模型完全无效。

### 实验2：标签校正后训练（本次）

| 项目 | 值 |
|:----|:----|
| 模型 | YOLOv8n |
| Epochs | 171（提前收敛，patience=50触发早停） |
| 最佳mAP50 | **0.991**（Epoch 155） |
| 最终mAP50 | 0.989 |
| mAP50-95 | 0.794 |
| 精确率(P) | 0.982 |
| 召回率(R) | 0.971 |

#### 训练曲线关键节点

```
Epoch   1 | mAP50=0.279 | loss=3.49
Epoch   2 | mAP50=0.676 | loss=1.85
Epoch   3 | mAP50=0.850 | loss=1.44
Epoch   5 | mAP50=0.913 | loss=1.13
Epoch  10 | mAP50=0.961 | loss=0.88
Epoch  20 | mAP50=0.981 | loss=0.65
Epoch  50 | mAP50=0.987 | loss=0.45
Epoch 100 | mAP50=0.989 | loss=0.39
Epoch 155 | mAP50=0.991 | loss=0.39 ← 最佳
Epoch 171 | mAP50=0.989 | loss=0.38 ← 早停
```

#### 测试图检测结果

**图片：** pcb_01275.jpg

| 类别 | GT标注 | 检测结果 | 置信度 | 判定 |
|:----|:-----:|:--------:|:-----:|:---:|
| open | ✅ | ✅ | 0.87 | HIT |
| mousebite | ✅ | ✅ | 0.88 | HIT |
| copper | ✅ | ✅ | 0.93 | HIT |
| pin-hole | ✅ | ✅ | 0.88 | HIT |
| short | 无 | 无 | — | ✅ 无误报 |
| spur | 无 | 无 | — | ✅ 无误报 |

**总检测：** 8个（去重后5个），**零误报，零漏检**

### 实验2 vs 实验1 对比

| 指标 | 实验1（错位标签） | 实验2（校正标签） | 提升 |
|:----|:---------------:|:----------------:|:---:|
| mAP50 | 0.279 | **0.991** | **+255%** |
| 单图检测数 | 219个误报 | 5个正确 | **-97.7%** |
| open误报 | 3个 | 0个 | ✅ |
| short误报 | 6个 | 0个 | ✅ |
| 过孔误标open | 是 | 否 | ✅ |
| 拐角误标short | 是 | 否 | ✅ |

---

## 模型导出

| 格式 | 文件 | 大小 |
|:----|:----|:----:|
| PyTorch | best.pt | 6.0MB |
| ONNX | best.onnx | 待导出 |
| 地平线BIN | xxx.bin | 待转换 |

### ONNX导出参数
- imgsz: 640
- half: False (FP32)
- simplify: True
- 输出格式: [1, 10, 8400]

### 地平线BIN转换参数
- march: bayes-e (RDK X5)
- 输入: RGB NCHW 1×3×640×640
- 归一化: data_scale (1/255)
- 校准数据: DeepPCB验证集50张
- 编译: O3优化, latency模式

---

## 实验3：合并数据集训练（PCBDatasets1 + DeepPCB）

| 项目 | 值 |
|:----|:----|
| 模型 | YOLOv8n |
| 训练集 | **10,342张**（DeepPCB 1,275 + PCBDatasets1 9,067） |
| 验证集 | **1,826张**（DeepPCB 225 + PCBDatasets1 1,601） |
| 总标注数 | **31,677个**（各类4,300~4,800，均衡分布） |
| 数据来源 | DeepPCB开源 + PCBDatasets1开源（类名映射转换） |
| Epochs | 200（patience=40早停） |
| Batch | 8（RTX4050 6GB） |
| 学习率 | 0.005（cos_lr） |
| 状态 | ⏳ 训练中 |

### PCBDatasets1 → DeepPCB 类别映射

PCBDatasets1是6类开源PCB缺陷数据，与DeepPCB的类别映射关系：

| PCBDatasets1原类 | → | DeepPCB类 |
|:---------------:|:---:|:---------:|
| 0=missing_hole | → | 5=pin-hole |
| 1=mouse_bite | → | 2=mousebite |
| 2=open_circuit | → | 0=open |
| 3=short | → | 1=short |
| 4=spur | → | 3=spur |
| 5=copper | → | 4=copper |

---

## 后处理规则更新（v2）

### 规则4：丝印字符缝隙 → 剔除pin-hole（新增）

**位置：** `scripts/detect.py` 第103-119行

**原理：** PCB丝印字符的笔画间隙在局部特征上与针孔（pin-hole）相似，容易被模型误检。规则4通过分析检测框周围邻域的灰度统计特征，区分铜箔区域的真实针孔与丝印文字区的假阳性。

**策略：**
- 置信度 ≥ 0.7 → 直接放行（模型确信度高的真实缺陷）
- 置信度 < 0.7 → 取检测框周围8px邻域，分析灰度和方差
  - 邻域均值 80~175 且 方差 > 30 → 判定为丝印文字区（亮字+暗底混杂）→ **剔除**
  - 否则 → 放行

**效果（226张测试集验证）：**

| 指标 | 无规则 | 有规则4 |
|:----|:-----:|:--------:|
| pin-hole检测总数（NMS前） | 2,341个 | — |
| pin-hole最终检测数 | — | **221个** |
| ✅ 精确率 | — | **96.4%** |
| ✅ 召回率 | — | **87.3%** |
| 误检过滤 | 大量 | **仅剩8个FP** |

---

### 实验4：合并数据集训练（pcb_v4，2026/08/02）

**起点：** `runs/runs/pcb_merged_v1/weights/best.pt`（epoch92）显式加载续训，绕开 resume 复用缓存坑

| 项目 | 值 |
|:----|:----|
| 模型 | YOLOv8n（从 pcb_merged_v1/best.pt 显式加载，nc=6） |
| 训练集 | 10,342张（DeepPCB 1,275 + PCBDatasets1 9,067） |
| 验证集 | 1,826张 |
| 输入尺寸 | **640×640**（保持，避免 1280 在 X5 上超限/掉帧） |
| Batch | 8（RTX 4050 Laptop 6GB） |
| Epochs | 180（patience=60，本次正常收敛，无早停截断） |
| lr0 | 0.005（cos_lr） |
| 数据增强 | hsv_h=0.1 / hsv_s=0.7 / hsv_v=0.4 / degrees=15 / mosaic=0.8 / mixup=0.2 等 |

**最终验证结果（1826张 / 4737实例）：**

| 类别 | Images | Instances | P | R | mAP50 | mAP50-95 |
|:----|:----:|:----:|:----:|:----:|:----:|:----:|
| **all** | 1826 | 4737 | **0.986** | **0.989** | **0.993** | **0.730** |
| open | 499 | 898 | 0.983 | 0.991 | 0.992 | 0.721 |
| short | 403 | 713 | 0.981 | 0.982 | 0.992 | 0.713 |
| mousebite | 445 | 800 | 0.987 | 0.985 | 0.993 | 0.721 |
| spur | 438 | 770 | 0.991 | 0.988 | 0.993 | 0.709 |
| copper | 459 | 784 | 0.989 | 0.995 | 0.993 | 0.764 |
| pin-hole | 471 | 772 | 0.987 | 0.991 | 0.992 | 0.756 |

**关键提升（实验3 vs 实验4）：**

| 指标 | 实验3（pcb_v3, 31轮早停） | 实验4（pcb_v4, 完整训练） | 提升 |
|:----|:---------------:|:----------------:|:---:|
| mAP50 | 0.991 | **0.993** | +0.002 |
| mAP50-95 | 0.670 | **0.730** | **+0.060** |

**单帧性能（PC端 RTX 4050）：** preprocess 1.0ms / inference 3.6ms / postprocess 1.0ms → 整链路约 5.6ms/帧

**结论：** 保持 640 输入 + 充分训练（patience=60），mAP50-95 从 0.67 提升到 0.73。copper/pin-hole 最优（0.76/0.756），spur 最弱（0.709，小目标定位难点）。验证了"640 + 更强模型"优于盲目提升分辨率的部署决策。

**坑记录（下次避开）：** resume=True 会复用 args.yaml 缓存的数据集（coco8），导致误训。正确做法是显式 `YOLO(权重路径)` + 显式传 `data=`，不用 resume。

---

## 待办

- [x] ~~ONNX导出 → PC端验证~~
- [ ] hb_mapper转BIN（pcb_v4 best.pt → ONNX → BIN）
- [ ] X5板端推理测试
- [x] ~~后处理规则调优~~ → 已完成：极小面积/过孔/拐角/丝印4条规则（R1-R9）
- [ ] TensorRT/ONNX Runtime对比实验
- [ ] 量化前后精度对比（FP32 vs INT8）
- [ ] 论文第四章（模型训练与部署）撰写
- [x] ~~数据集合并（PCBDatasets1 + DeepPCB）~~
- [x] ~~合并数据集训练完成验证~~ → 实验4完成，mAP50=0.993 / mAP50-95=0.730

---

## 2026/08/11 pin-hole 被杀根因 + 修复记录

**现象：** 用 pcb_v4 跑 235 张测试图，带后处理规则时 pin-hole 检出 **0 个**，无规则时 253 个；mousebite 126 vs 271，open 202 vs 299 —— 规则大量误杀。

**根因：** `scripts/detect.py` 后处理循环里

```python
x1, y1, x2, y2, sc, ci = d.astype(int)   # 整行转 int
```

`d` 是 `[x1,y1,x2,y2,conf,cls]` 的 float 行，`astype(int)` 把置信度 `conf`（如 0.89）截断成 **0**。导致所有 `sc < 阈值` 的低置信度过滤分支**恒真**：
- R8a `sc < 0.40 → 剔除` → pin-hole 253 个全被杀光
- R9 `sc < 0.5` → mousebite 误杀 145 个
- R5b/R6/R7 的 `sc<0.45/0.5/0.55` 全部失效 → open/copper 连坐

**修复：** 坐标取整、置信度保留小数

```python
x1, y1, x2, y2 = d[:4].astype(int)   # 坐标取整
sc = float(d[4])                      # 置信度保留小数!
ci = int(d[5])
```

同时修了 main() 里两处画框的同样写法（否则标签置信度显示 0.00）。

**修复后验证（pcb_v4 + 9条规则，235张测试图）：**

| 类别 | 带规则 | 无规则 | 规则删掉 |
|:----:|:-----:|:-----:|:-------:|
| copper | 217 | 217 | 0 |
| mousebite | 268 | 271 | 3 |
| open | 297 | 299 | 2 |
| pin-hole | **253** | 253 | **0（全保留）** |
| short | 221 | 222 | 1 |
| spur | 229 | 230 | 1 |
| **合计** | **1485** | **1492** | **7** |

**结论：**
1. pin-hole 不是"识别难、置信度低"，而是 bug 把置信度截成 0 才被规则误杀。pcb_v4 的 pin-hole 置信度中位数 0.89、97.6% ≥0.70，修复后全部正常保留，**决定保留 pin-hole 类别**。
2. 规则本身对 pcb_v4 有效但触发性变低（模型整体置信度高，低置信度过滤分支基本不触发）——后续若换摄像头实拍需按实际置信度分布重新标定规则阈值。

**关联：** 规则阈值调参基准是早期模型，见 [[bishe-code-deep-dive-detect]] 的 R1-R9 分析。修复文件：`scripts/detect.py`（后处理循环 + 2处画框）。

---

## 2026/08/12 合并数据集混淆矩阵验证（系统验证数据）

**目的：** 用带 GT 的验证集精确评估 pcb_v4，确认是否真实存在"类别系统性判错"。

**前提修正（重要）：**
1. 数据集 = **合并数据集**（`D:/DeepPCB_YOLO`，DeepPCB 1275 + PCBDatasets1 9067 → train 10342 / val 1826）
2. 150轮模型 = **pcb_v4**（8/2 训练，180轮 patience=60 完整收敛）
3. test_images 里的 `l_light_01_*`、`rotation_90_light_09` 等是**测试用散图，不在合并数据集的 train/val 中**——它们检错属分布外样本退化，不代表训练问题

**评估工具修复（2个脚本 bug）：**
- `scripts/eval_confusion.py` / `scripts/eval_rules.py`：GT 坐标固定 640 换算 → 改为按每张图实际尺寸（验证集混合 600×600 的 PCBDatasets1 和 640×640 的 DeepPCB）
- `Path.glob()` 在本机环境失效返回空 → 改用 `os.listdir`

**验证结果（1826张含GT，conf=0.5，无规则）：**

| 类别 | TP | FP | FN | P | R | F1 |
|:----|:--:|:--:|:--:|:--:|:--:|:--:|
| open | 870 | 34 | 28 | 0.962 | 0.969 | 0.966 |
| short | 695 | 9 | 18 | 0.987 | 0.975 | 0.981 |
| mousebite | 776 | 10 | 24 | 0.987 | 0.970 | 0.979 |
| spur | 756 | 14 | 14 | 0.982 | 0.982 | 0.982 |
| copper | 767 | 14 | 17 | 0.982 | 0.978 | 0.980 |
| pin-hole | 741 | 16 | 31 | 0.979 | 0.960 | 0.969 |

**混淆矩阵（行=GT，列=预测）：** 几乎全对角，最大跨类混淆仅 mousebite→open 2 个、pin-hole→mousebite 1 个。**不存在系统性类别错位。**

**结论：**
1. pcb_v4 在合并数据集验证集上 P/R/F1 全部 0.96+，**训练充分、无类别混淆问题**
2. pin-hole P=0.979/R=0.960/F1=0.969 —— **表现正常，之前"被删光"确系置信度截断 bug，已修复**
3. 真实短板不在训练，在"**模型未见过的真实相机实拍图**"（分布外退化）→ 对应"先部署再迭代"路线：X5 实拍后定向补数据增量训练
